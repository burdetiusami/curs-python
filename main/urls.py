from django.contrib import admin
from django.urls import include, path
from main.views import homepage, employees_index, orders_index, logged_home, fleet_index

app_name = 'main'

urlpatterns = [
    
]